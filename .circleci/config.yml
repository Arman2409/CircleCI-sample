version: 2.1
jobs:
  test: 
    docker:
     - image: node:aiplane
    steps:
        - checkout
        - run: npm run build
  build: 
    docker:
      - image: node:aiplane
    steps: 
      - run: npm run build
  deploy:
    docker:
      - image: node:aiplane
    steps:
      - command: git push heroku master