version: 2.1
jobs:
  test: 
    docker:
     - image: node:alplane
    steps:
        - checkout
        - run: npm run build
  build: 
    docker:
      - image: node:alplane
    steps: 
      - run: npm run build
  deploy:
    docker:
      - image: node:alplane
    steps:
      - command: git push heroku master